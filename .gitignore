# Compiled binary
.mcp-host-bin

# Runtime files generated at startup
.mcp-host.pid
.mcp-host.log

# Editor / OS
.DS_Store
*.swp
*.swo
*~

# Go build cache
/vendor/

# Config contains API keys — use config.yaml.example as template
config.yaml

# Service account key files — never commit credentials
*.json
